<Window x:Class="WpfApplication1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
         xmlns:self="clr-namespace:WpfApplication1"
        mc:Ignorable="d"
        Title="MainWindow" Height="830.579" Width="744">
    <Grid Margin="10">
        <TreeView Name="TreeView0" >
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate DataType="{x:Type self:MenuItem}" ItemsSource="{Binding Items}">
                    <TextBlock Text="{Binding Title}"></TextBlock>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
        <!--<ListView Grid.Column="0" Name="ListView0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="SharePoint"></TextBlock>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <ListView Grid.Column="1" Name="ListView1"></ListView>
        <ListView Grid.Column="2" Name="ListView2"></ListView>
        <ListView Grid.Column="3" Name="ListView3"></ListView>
        <ListView Grid.Column="4" Name="ListView4"></ListView>-->
    </Grid>
</Window>
